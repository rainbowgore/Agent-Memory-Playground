# Backend API URL (used by the frontend to call the FastAPI server).
# Default if unset: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: GitHub repo URL for the corner link
# NEXT_PUBLIC_GITHUB_URL=https://github.com/your-username/agent-memory-playground
